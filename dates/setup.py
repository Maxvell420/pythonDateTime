from setuptools import setup, find_packages

setup(
    name="dates",
    version="0.1.0",
    author="Velichko M.A.",
    description="Даты и интервал времени",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.6",
)
